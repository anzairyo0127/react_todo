export enum TodoAction {
  ADD,
  TO_WORKING,
  TO_COMPLEATED,
  DELETE_TODO
}

export enum VisualFilter {
  SHOW_ALL,
  SHOW_COMP,
  SHOW_ACTIVE
}

export enum TodoStatus {
  NEW,
  WAITING,
  WORKING,
  COMPLEATED
}
